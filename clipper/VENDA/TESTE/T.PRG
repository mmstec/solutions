 CLS
 USE DBVALOR ;PACK
 INDEX on STR(VALOR)+STR(COMISSAO)+DTOS(DATA)+LKCONTA+LKCAIXA to DBVALOR1.NTX 
 APAGADO=0
 GOTO TOP
 DO WHILE !EOF()
    IF VALOR=0 .AND. COMISSAO=0
       APAGADO++
       DELE
    ENDIF
    SKIP
 ENDDO
 ?""
 ?"QUANTIDADE DE REGISTROS ELIMINADOS:"+STR(APAGADO)
